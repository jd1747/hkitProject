# backend - packages 설치
* pip install uv (전역)
* cd backend > uv init
* (backend 위치에서) uv sync

# 실행 (개발 단계)
* (backend: cd backend) > python main.py 혹은 main.py 파일에서 코드 실행
* (frontend: cd frontend) > npm run dev

# Note
* main.py 내용을 app/app.py로 옮기기만 했고, 그 외 파일들은 임시로 만들어놓기만 했어요.